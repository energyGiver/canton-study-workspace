---
title: "0.10.0"
description: "Wallet SDK 0.10.0 release note"
source_id: "SRC-A79FAC8A65"
source_path: "integrations/release-notes/wallet-sdk-releases/0-10-0"
source_commit: "5ce61f7ca8ec6ad9af3d5e19db3583588ba49d65"
source_sha256: "3ba8b6e3a3e60d10a69e47d0ba072c3faf30443cbbdc0b2aaa6051728cb06f56"
translation_status: "ai_draft"
translated_at: "2026-08-12"
reviewed_by: null
---

<Warning>
  이 페이지는 팀 리서치를 위한 비공식 한글 번역입니다. 기술적 판단에는 [공식 영어 원문](https://docs.canton.network/integrations/release-notes/wallet-sdk-releases/0-10-0.md)을 사용하세요.
</Warning>

{/* GENERATED_RELEASE_NOTES target="wallet-sdk" format="split-release" version="0.10.0" source="158b3ff4c316ba7a5d6c89a6099a5303b76d4f77" */}

# 0.10.0

**2025년 10월 2일 release**

- featured app right 자체 발행

*이제 Internal Party와 External Party 모두 wallet UI와 비슷하게 자신에게 featured app right를 부여할 수 있습니다.*

```javascript
// For external parties
const [command,disclosedContracts] = sdk.tokenStandard!.selfGrantFeatureAppRights()

await sdk.userLedger?.prepareSignExecuteAndWaitFor(
    command,
    keyPair.privateKey,
    v4(),
    disclosedContracts
)

// For internal parties
await sdk.tokenStandard!.grantFeatureAppRightsForInternalParty()
```

- AppProvider 및 AppUser용 LocalNet variation

*이제 두 Validator 사이의 operation을 보여 주기 위해 AppProvider와 AppUser를 모두 쉽게 사용할 수 있습니다.*

```javascript
    const providerSDK = new WalletSDKImpl().configure({
        logger,
        authFactory: localNetAuthDefault,
        ledgerFactory: localNetLedgerAppProvider, //new variations here
        topologyFactory: localNetTopologyAppProvider, //new variations here
        tokenStandardFactory: localNetTokenStandardAppProvider, //new variations here
        validatorFactory: localValidatorDefault,
    })

    const userSDK = new WalletSDKImpl().configure({
        logger,
        authFactory: localNetAuthDefault,
        ledgerFactory: localNetLedgerAppUser, //new variations here
        topologyFactory: localNetTopologyAppUser, //new variations here
        tokenStandardFactory: localNetTokenStandardAppUser, //new variations here
        validatorFactory: localValidatorDefault,
    })
```

*LocalNet..Default도 계속 존재하며 이전처럼 AppUser Validator를 default로 사용합니다.*

- Topology Transaction hash 재계산

*이제 hash를 다시 계산하여 offline에서 Topology Transaction을 validate할 수 있습니다.*

```javascript
const recomputeHash = await TopologyController.computeTopologyTxHash(
    prepared!.partyTransactions
)

if (recomputeHash !== prepared!.combinedHash) {
    throw new Error(
        'Recomputed hash does not match prepared combined hash'
    )
}
```

- `prepareSignExecuteAndWaitFor` 및 `executeSubmissionAndWaitFor`를 사용하는 새 await variation

*release 0.7.0에서 `waitForCompletion`을 도입했으며 이제 execution에 포함했습니다.*

```javascript
// PREVIOUS CODE EXAMPLE
//it is recommended to fetch ledger offset before preparing your command
const offsetLatest = (await sdk.userLedger?.ledgerEnd())?.offset ?? 0

const transferCommandId =
    // prepareSignAndExecuteTransaction & prepareSign now returns the commandId
    await sdk.userLedger?.prepareSignAndExecuteTransaction(
        [{ ExerciseCommand: transferCommand }],
        keyPairSender.privateKey,
        v4(),
        disclosedContracts2
    )

//new command that scans the ledger to ensure the command have completed
const completion = await sdk.userLedger?.waitForCompletion(
    offsetLatest, //where to start from
    5000, //optional timeout in ms
    transferCommandId! //the command to look for
)

// NEW VARIATION
const completion =
        await sdk.userLedger?.prepareSignExecuteAndWaitFor(
            transferCommand,
            keyPairSender.privateKey,
            v4(),
            disclosedContracts,
            10000 // 10 second timeout, if no value is provided here a default of 15 seconds is used
        )

// VARIATION FOR `ExecuteSubmission`
const completion =
        await onlineSDK.userLedger?.executeSubmissionAndWaitFor(
            transferCommand,
            signedHash,
            keyPairSender.publicKey,
            v4()
        )
```

- `executeSubmission`이 이제 `prepareSignAndExecuteTransaction`과 마찬가지로 submissionId를 반환합니다
- `TopologyController.createTransactionHash` 사용 시 seed가 없을 때 throw되는 exception을 수정했습니다
- `prepareSubmission`이 이제 `prepareSignAndExecuteTransaction`과 같은 Command input signature를 가집니다
